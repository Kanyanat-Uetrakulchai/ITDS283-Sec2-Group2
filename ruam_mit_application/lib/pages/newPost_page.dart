import 'package:flutter/material.dart';

class NewpostPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('New Post page')),
      body: Center(child: Text('New Post page', style: TextStyle(fontSize: 24))),
    );
  }
}
